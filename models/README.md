# Models

Store trained model artifacts in this folder when publishing reproducible versions.

Examples:
- `recommender.pkl`
- `model_metadata.json`

Avoid committing large binary artifacts unless required; prefer release assets or object storage for production.
